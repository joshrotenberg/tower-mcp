[project]
name = "tower-mcp-python-sdk-interop"
version = "0.1.0"
description = "Official Python MCP SDK interoperability fixtures for tower-mcp"
requires-python = ">=3.12"
dependencies = [
    "mcp==2.0.0",
]

[tool.uv]
package = false
